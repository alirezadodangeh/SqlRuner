<Window x:Class="SqlRuner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SqlRuner"
        Title="SQL Runner" Height="700" Width="1400"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <local:BoolToBrushConverter x:Key="BoolToBrush"/>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <local:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility"/>
        <local:NullToVisibilityConverter x:Key="NullToVisibility"/>
        <local:BoolToStatusColorConverter x:Key="BoolToStatusColor"/>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10*" MinWidth="400"/>
            <ColumnDefinition Width="448*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="229*" MinWidth="300"/>
        </Grid.ColumnDefinitions>

        <!-- Main Content Area -->
        <Grid Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,5,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Connection String Section -->
            <GroupBox Header="Connection String" Grid.Row="0" Margin="0,0,0,10">
                <StackPanel>
                    <TextBox x:Name="ConnectionStringTextBox" 
                             Text="Data Source=DESKTOP-L87JH9I\\SQL2022;Initial Catalog=Portal_NewBpms_34;Integrated Security=True;MultipleActiveResultSets=true;TrustServerCertificate=Yes;Connect Timeout=10"
                             Padding="5" 
                             FontSize="12"
                             VerticalContentAlignment="Center"
                             Height="35"
                             ToolTip="برای SQL Server Named Instance: Data Source=سرور\\Instance;Initial Catalog=Database;Integrated Security=True;&#x0a;نکته: از \\ برای backslash استفاده کنید (در XAML به صورت \\\)"/>
                    <TextBlock Text="مثال SQLite: Data Source=C:\database.db;Version=3; | مثال SQL Server: Server=localhost;Database=MyDB;User Id=sa;Password=123;" 
                               FontSize="9" 
                               Foreground="Gray" 
                               TextWrapping="Wrap"
                               Margin="5,2,5,0"/>
                </StackPanel>
            </GroupBox>

            <!-- Query Section -->
            <GroupBox Header="SQL Query" Grid.Row="1" Margin="0,0,0,10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBox x:Name="QueryTextBox" 
                             Text="select * from sys.tables"
                             TextWrapping="Wrap" 
                             AcceptsReturn="True" 
                             AcceptsTab="True"
                             VerticalScrollBarVisibility="Auto"
                             FontFamily="Consolas"
                             FontSize="13"
                             Padding="5"
                             MinHeight="150"
                             Grid.Row="0"/>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button x:Name="TestConnectionButton" 
                                Content="🔌 تست اتصال" 
                                Height="40"
                                Width="120"
                                Margin="0,0,10,0"
                                FontSize="13"
                                Click="TestConnectionButton_Click"/>
                        <Button x:Name="ExecuteButton" 
                                Content="▶️ اجرای کوئری" 
                                Height="40"
                                Width="140"
                                FontSize="14"
                                FontWeight="Bold"
                                Click="ExecuteButton_Click"/>
                    </StackPanel>
                </Grid>
            </GroupBox>

            <!-- Results Section -->
            <GroupBox Header="نتایج" Grid.Row="2" Margin="0,0,0,10">
                <DataGrid x:Name="ResultsDataGrid" 
                          AutoGenerateColumns="True"
                          IsReadOnly="True"
                          GridLinesVisibility="All"
                          HeadersVisibility="Column"
                          AlternatingRowBackground="#F5F5F5"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          SelectionMode="Extended"/>
            </GroupBox>

            <!-- Status Bar -->
            <StatusBar Grid.Row="3" Height="30">
                <StatusBarItem>
                    <TextBlock x:Name="StatusTextBlock" Text="آماده"/>
                </StatusBarItem>
            </StatusBar>
        </Grid>

        <!-- Splitter -->
        <GridSplitter Grid.Column="2" 
                      Width="5" 
                      HorizontalAlignment="Center"
                      Background="LightGray"
                      ResizeBehavior="PreviousAndNext"/>

        <!-- History Panel -->
        <Grid Grid.Column="3" Margin="10,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <GroupBox Header="تاریخچه کوئری‌های اجرا شده" Grid.Row="0">
            </GroupBox>

            <DataGrid x:Name="HistoryDataGrid" 
                     Grid.Row="1"
                     Margin="0,10,0,0"
                     AutoGenerateColumns="False"
                     IsReadOnly="True"
                     SelectionMode="Single"
                     SelectionChanged="HistoryDataGrid_SelectionChanged"
                     MouseDoubleClick="HistoryDataGrid_MouseDoubleClick"
                     GridLinesVisibility="All"
                     HeadersVisibility="Column"
                     AlternatingRowBackground="#F5F5F5"
                     CanUserAddRows="False"
                     CanUserDeleteRows="False"
                     RowBackground="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="تاریخ/زمان" 
                                       Binding="{Binding ExecutedAt, StringFormat='{}{0:yyyy/MM/dd HH:mm:ss}'}" 
                                       Width="140"
                                       FontSize="10"/>
                    <DataGridTextColumn Header="وضعیت" 
                                       Binding="{Binding Status}" 
                                       Width="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{Binding IsSuccessful, Converter={StaticResource BoolToStatusColor}}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="تعداد رکورد" 
                                       Binding="{Binding RecordCountDisplay}" 
                                       Width="90"
                                       FontFamily="Consolas"
                                       FontSize="11">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="{Binding IsSuccessful, Converter={StaticResource BoolToStatusColor}}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="کوئری" 
                                       Binding="{Binding Query}" 
                                       Width="*"
                                       FontFamily="Consolas"
                                       FontSize="10">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextWrapping" Value="Wrap"/>
                                <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                <Setter Property="ToolTip" Value="{Binding Query}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="{Binding IsSuccessful, Converter={StaticResource BoolToBrush}}"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#BBDEFB"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
        </Grid>
    </Grid>
</Window>

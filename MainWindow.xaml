<Window x:Class="SqlRuner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SqlRuner"
        Title="SQL Runner" Height="700" Width="1400"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <local:BoolToBrushConverter x:Key="BoolToBrush"/>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <local:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility"/>
        <local:NullToVisibilityConverter x:Key="NullToVisibility"/>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" MinWidth="400"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*" MinWidth="300"/>
        </Grid.ColumnDefinitions>

        <!-- Main Content Area -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Connection String Section -->
            <GroupBox Header="Connection String" Grid.Row="0" Margin="0,0,0,10">
                <StackPanel>
                    <TextBox x:Name="ConnectionStringTextBox" 
                             Text="Data Source=DESKTOP-L87JH9I\\SQL2022;Initial Catalog=Portal_NewBpms_34;Integrated Security=True;MultipleActiveResultSets=true;TrustServerCertificate=Yes;Connect Timeout=10"
                             Padding="5" 
                             FontSize="12"
                             VerticalContentAlignment="Center"
                             Height="35"
                             ToolTip="برای SQL Server Named Instance: Data Source=سرور\\Instance;Initial Catalog=Database;Integrated Security=True;&#x0a;نکته: از \\ برای backslash استفاده کنید (در XAML به صورت \\\)"/>
                    <TextBlock Text="مثال SQLite: Data Source=C:\database.db;Version=3; | مثال SQL Server: Server=localhost;Database=MyDB;User Id=sa;Password=123;" 
                               FontSize="9" 
                               Foreground="Gray" 
                               TextWrapping="Wrap"
                               Margin="5,2,5,0"/>
                </StackPanel>
            </GroupBox>

            <!-- Query Section -->
            <GroupBox Header="SQL Query" Grid.Row="1" Margin="0,0,0,10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBox x:Name="QueryTextBox" 
                             Text="select * from sys.tables"
                             TextWrapping="Wrap" 
                             AcceptsReturn="True" 
                             AcceptsTab="True"
                             VerticalScrollBarVisibility="Auto"
                             FontFamily="Consolas"
                             FontSize="13"
                             Padding="5"
                             MinHeight="150"
                             Grid.Row="0"/>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button x:Name="TestConnectionButton" 
                                Content="🔌 تست اتصال" 
                                Height="40"
                                Width="120"
                                Margin="0,0,10,0"
                                FontSize="13"
                                Click="TestConnectionButton_Click"/>
                        <Button x:Name="ExecuteButton" 
                                Content="▶️ اجرای کوئری" 
                                Height="40"
                                Width="140"
                                FontSize="14"
                                FontWeight="Bold"
                                Click="ExecuteButton_Click"/>
                    </StackPanel>
                </Grid>
            </GroupBox>

            <!-- Results Section -->
            <GroupBox Header="نتایج" Grid.Row="2" Margin="0,0,0,10">
                <DataGrid x:Name="ResultsDataGrid" 
                          AutoGenerateColumns="True"
                          IsReadOnly="True"
                          GridLinesVisibility="All"
                          HeadersVisibility="Column"
                          AlternatingRowBackground="#F5F5F5"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          SelectionMode="Extended"/>
            </GroupBox>

            <!-- Status Bar -->
            <StatusBar Grid.Row="3" Height="30">
                <StatusBarItem>
                    <TextBlock x:Name="StatusTextBlock" Text="آماده"/>
                </StatusBarItem>
            </StatusBar>
        </Grid>

        <!-- Splitter -->
        <GridSplitter Grid.Column="1" 
                      Width="5" 
                      HorizontalAlignment="Center" 
                      VerticalAlignment="Stretch"
                      Background="LightGray"
                      ResizeBehavior="PreviousAndNext"/>

        <!-- History Panel -->
        <Grid Grid.Column="2" Margin="10,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <GroupBox Header="تاریخچه کوئری‌های اجرا شده" Grid.Row="0">
            </GroupBox>
            
            <ListBox x:Name="HistoryListBox" 
                     Grid.Row="1"
                     Margin="0,10,0,0"
                     SelectionChanged="HistoryListBox_SelectionChanged"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#E0E0E0" 
                                BorderThickness="0,0,0,1" 
                                Padding="8,8,8,8"
                                Margin="0,0,0,5"
                                CornerRadius="3">
                            <Border.Background>
                                <Binding Path="IsSuccessful" Converter="{StaticResource BoolToBrush}"/>
                            </Border.Background>
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                    <TextBlock Text="{Binding ExecutedAt, StringFormat='📅 {0:yyyy/MM/dd HH:mm:ss}'}" 
                                               FontWeight="Bold" 
                                               FontSize="11"
                                               Foreground="#666666"/>
                                    <TextBlock Text=" ✓" 
                                               Foreground="Green"
                                               FontWeight="Bold"
                                               Visibility="{Binding IsSuccessful, Converter={StaticResource BoolToVisibility}}"/>
                                    <TextBlock Text=" ✗" 
                                               Foreground="Red"
                                               FontWeight="Bold"
                                               Visibility="{Binding IsSuccessful, Converter={StaticResource InverseBoolToVisibility}}"/>
                                </StackPanel>
                                <TextBlock Text="{Binding Query}" 
                                           TextWrapping="Wrap"
                                           FontFamily="Consolas"
                                           FontSize="11"
                                           MaxHeight="60"
                                           TextTrimming="CharacterEllipsis"
                                           ToolTip="{Binding Query}"/>
                                <TextBlock Text="{Binding ConnectionString, StringFormat='🔗 {0}'}" 
                                           FontSize="9"
                                           Foreground="#888888"
                                           TextTrimming="CharacterEllipsis"
                                           Margin="0,3,0,0"
                                           ToolTip="{Binding ConnectionString}"/>
                                <TextBlock Text="{Binding ErrorMessage, StringFormat='❌ خطا: {0}'}" 
                                           FontSize="10"
                                           Foreground="Red"
                                           TextWrapping="Wrap"
                                           MaxHeight="40"
                                           TextTrimming="CharacterEllipsis"
                                           Margin="0,3,0,0"
                                           Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibility}}"
                                           ToolTip="{Binding ErrorMessage}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,0,0,2"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Grid>
    </Grid>
</Window>
